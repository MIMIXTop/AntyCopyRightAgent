{
  "type": "function",
  "function": {
    "name": "get_students_list",
    "description": "Retrieves a list of students for the specified course in Google Classroom. Returns IDs, Google profile data, and a link to their Google Drive folder. Never invent a `course_id`! If you do not know it, call `get_courses` first.",
    "parameters": {
      "type": "object",
      "properties": {
        "course_id": {
          "type": "string",
          "description": "Unique course identifier (ID) in Google Classroom."
        }
      },
      "required": ["course_id"]
    }
  }
}